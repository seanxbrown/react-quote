import Row from "react-bootstrap/Row"
export const Header = () => {
    return(
        <Row id="header"className="text-center">
            <header>Inspiration Bank</header>
            <p>Search for quotes from your favourite historical figures then save them for future reference</p>
        </Row>
    )
}

